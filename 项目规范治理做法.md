# 项目规范治理做法

同一类错误反复出现时，不要继续催着把当前任务做完。先停、先复盘、先把规则和流程改落地，再回去接着干。否则同样的坑会跟着任务一起放大。

---

## 怎么用

触发条件：AI 多次在相同或相关事情上犯错（同一条约束、同一类文件、同一种交互、同一种漏检）。一旦对上，尽快暂停当前工作，而不是「这次先改完再说」。

顺序固定：**暂停 → 复盘 → 落地改进 → 再继续被暂停的工作**。中间不要夹带原任务的推进。

---

## 做法

### 1. 尽快暂停

发现重复犯错就停。当前半成品可以留着，但不再让 AI 在同一套错误习惯上继续写。

可直接丢给 AI：

```
先暂停当前工作，不要继续改功能 / 补代码。
后面只做复盘和规范落地，等改进落地后再恢复被暂停的任务。
```

### 2. 复盘，梳出规则、规范、工作流改进

让 AI 把「反复错在哪、为什么会重复、下次靠什么拦住」写清楚。产出应落到可执行的东西上，而不是只道歉。

要梳出来的通常包括：

- 规则：以后必须遵守 / 禁止做什么
- 规范：命名、目录、接口、提交、文档等约定
- 工作流改进：检查点、清单、技能、钩子、评审步骤等，用来防止再漏

可直接丢给 AI：

```
请复盘：你多次在相同或相关事情上犯错。
1. 列出每次分别错在哪，它们的共同点是什么。
2. 说明为什么会重复发生（缺规则、规则写了但没进工作流、还是执行时没自检）。
3. 给出可落地的规则、规范、工作流改进方案。改进要能拦住下一次同类错误，而不是只描述原则。
```

### 3. 先落地改进，再继续被暂停的工作

改进没写进仓库、技能、规则文件或检查流程之前，不要恢复原任务。落地后再从暂停点继续，并明确要求按新规则执行。

可直接丢给 AI：

```
先落地刚才梳出来的规则、规范和工作流改进（写入对应文件 / 清单 / 技能）。
确认已经能在后续任务里自动生效后，再继续之前被暂停的工作。
恢复工作时，按新规则执行，不要沿用暂停前的做法。
```

---

## 分条说明

| 目的 | 做法 |
|------|------|
| 打断重复犯错的惯性 | 发现 AI 多次在相同或相关事情上犯错，尽快暂停当前工作。 |
| 把教训变成可执行约束 | 让其复盘，梳理相关规则、规范、工作流改进方案。 |
| 避免「复盘完立刻再踩」 | 先落地这些改进，再继续被暂停的工作。 |
