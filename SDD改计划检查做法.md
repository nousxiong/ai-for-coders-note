# SDD 改计划检查做法

在 SDD 方式下，每当 AI 在实现过程中停下来、请求修改 plan 时（这种情况往往意味着 AI 自身开发遇到了无法解决的问题或发现 plan 的疏漏），不要直接同意改 plan。先检查：是需求和 AI 没有沟通清楚，还是需求过于复杂难以实现。

---

## 怎么用

触发条件：AI 在实现过程中停下来、请求修改 plan。一旦对上，先做原因检查，再决定怎么处理，而不是直接让它改 plan 继续写。

处理分两支：如果是前者（需求和 AI 没有沟通清楚），重新和 AI 对齐需求；如果是后者（需求过于复杂难以实现），可能需要适当简化需求。

---

## 做法

### 1. 把“请求改 plan”当检查信号

AI 在实现过程中停下来、请求修改 plan，往往意味着 AI 自身开发遇到了无法解决的问题或发现 plan 的疏漏。这是一个要检查需求的信号，不是直接改 plan 就能过去的小事。

可直接丢给 AI：

```
先不要改 plan。请先说明：你为什么要停下来请求修改 plan？
1. 是实现过程中遇到了无法解决的问题，还是发现了 plan 的疏漏？
2. 具体卡在哪一步、哪一条需求或哪一段 plan 上？
```

### 2. 检查是哪一类原因：没对齐，还是太复杂

对照下表判断当前属于哪一类：

| 原因 | 表现 | 后续处理 |
|------|------|----------|
| 需求和 AI 没有沟通清楚 | AI 理解的需求和你想的不一致、plan 里漏了关键约束或边界 | 重新和 AI 对齐需求 |
| 需求过于复杂难以实现 | 需求本身是对的，但步骤太多、依赖太深，AI 实现时反复卡住 | 适当简化需求 |

可直接丢给 AI：

```
请判断本次请求修改 plan 的原因属于哪一类：
1. 需求和你没有沟通清楚（你理解的需求、plan 和我的预期不一致）？
2. 还是需求过于复杂难以实现（需求本身没错，但实现复杂度太高）？
请给出判断依据，再按对应分支处理：前者重新对齐需求，后者给出简化需求的建议。
```

### 3. 分情况处理：对齐需求，或简化需求

- 如果是前者：重新和 AI 对齐需求。把没讲清楚的约束、边界、预期讲到位，对齐后再让 AI 改 plan。
- 如果是后者：可能需要适当简化需求。先降复杂度，再让 AI 改 plan，不要让 AI 在过复杂的需求上硬写。

可直接丢给 AI（对齐需求用）：

```
这次是需求没和你沟通清楚，我们重新对齐需求，暂不改 plan：
1. 先复述你当前理解的需求和 plan。
2. 我会纠正其中理解偏了、漏了约束或边界的部分。
3. 对齐确认无误后，你再按对齐后的需求修改 plan。
```

可直接丢给 AI（简化需求用）：

```
这次是需求过于复杂难以实现，先简化需求，暂不改 plan：
1. 列出当前需求里导致实现卡住的复杂点。
2. 给出简化方案（砍范围、拆步骤、降约束），并说明每项简化舍掉了什么。
3. 等我确认简化方案后，你再按简化后的需求修改 plan。
```

---

## 分条说明

| 目的 | 做法 |
|------|------|
| 接住“改 plan 请求”背后的真正信号 | AI 在实现过程中停下来、请求修改 plan 时，往往意味着 AI 自身开发遇到了无法解决的问题或发现 plan 的疏漏，要先检查原因。 |
| 区分是沟通问题还是复杂度问题 | 检查是否需求和 AI 没有沟通清楚、或者过于复杂难以实现。 |
| 沟通不清楚时回到同一起点 | 如果是前者，重新和 AI 对齐需求，对齐后再改 plan。 |
| 太复杂时先降难度再动手 | 如果是后者，可能需要适当简化需求，简化后再改 plan。 |
