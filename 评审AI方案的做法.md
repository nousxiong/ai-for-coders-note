# 评审 AI 方案的做法

理解方案之后，用下面几条去压测，而不是只看它写得是否自洽。重点查：尺度能不能撑到真实需求、该用的成熟办法有没有被绕开、用具体场景能不能把方案打穿，并对回答里的做法持续质疑。

把 `xxx` 换成你认为更合适的方案 / 办法 / 方式。

---

## 怎么用

先对照需求核对尺度，再决定要不要质疑「为什么不用 xxx」，再用例子和反例让 AI 自己评估。任何一轮回答里只要出现具体做法，就继续「刨根问底」。几条可以一起用，也可以只抓当前最可疑的那条。跨切片、跨批次、跨阶段这类「当前刚好卡在上限」的情况，优先查。

---

## 做法

### 1. 核对关键尺度是否撑得住需求

检查方案中关键尺度（比如单位数量、算法的层数）的适用范围是否符合需求。不符合就要提出质疑。

不符合的典型样子：某算法只能处理 6 个单位，超过 6 个则无法处理；当前切片是 6 个单位，但后续切片很快要提升到 30 个。尤其要检查跨切片情况。

可直接丢给 AI：

```
请核对方案中关键尺度（单位数量、算法层数等）的适用范围是否符合需求。
如果上限刚好等于当前切片/批次规模，请单独评估后续扩到更大规模、以及跨切片时是否仍然成立。
不符合需求时，明确指出卡在哪一个尺度上。
```

### 2. 发现没用 xxx 时，追问为什么不用

前提：你已经看出 AI 方案明显没有使用某个更合适的方案、办法或方式。没有这个前提就不要空问。

可直接丢给 AI：

```
为什么不使用 xxx 方案 / 办法 / 方式？
请对比当前方案和 xxx：适用边界、复杂度、和本需求的匹配程度。
```

### 3. 用例子推演，用反例压方案

举例子推演，并举出可能的反例，让 AI 用它自己的方案评估能否满足。问到具体位置、顺序、相对关系，看会不会自相矛盾。

可直接丢给 AI：

```
请评估以下场景：a 移动到了 x，b 在 a 的右侧。
按照你的方案能否处理？是否有矛盾？
如果能处理，请逐步推演；如果不能，指出方案在哪一步失效。
```

场景按你的业务替换。关键是：具体、可判定对错，而不是再让它讲一遍原理。

### 4. 刨根问底

对任何问题，都根据回答持续质疑其中的做法：提反例、找矛盾点。不要停在第一轮看起来说得通的地方；回答里新冒出来的步骤、假设、边界，接着压。

一轮问完，从回答里抽出做法再问下一轮，直到做法被打穿，或反例、矛盾都被明确消掉。

可直接丢给 AI：

```
针对你刚才说的做法，继续质疑，不要只复述原方案。
1. 给出可能让该做法失效的反例。
2. 指出做法内部、或做法与需求之间的矛盾点。
3. 对每个反例 / 矛盾：按你的方案逐步推演，说明能否处理；不能处理就承认失效点。
下一次回答里如果又出现新的做法或假设，继续按上面三条质疑。
```

---

## 分条说明

| 目的 | 做法 |
|------|------|
| 防止「当前刚好能跑、一扩就垮」 | 核对关键尺度（单位数量、算法层数等）是否符合需求；当前卡在上限、后续还要放大、尤其跨切片时，提出质疑。 |
| 防止绕开更合适的成熟办法 | 仅在明显没用 xxx 时追问：为什么不使用 xxx 方案、办法、方式？ |
| 防止纸面自洽、一落地就矛盾 | 举例子推演、给出可能反例，让 AI 用自己的方案评估能否处理、有没有矛盾。 |
| 防止第一轮说通就停 | 「刨根问底」：对任何问题，根据回答持续质疑其中做法（提反例、找矛盾点）。 |
